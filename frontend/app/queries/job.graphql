query GetJob($jobId: ID!) {
  job(where: { id: $jobId }) {
    title
    url
    content {
      document
    }
    startDate
    endDate
    company {
      name
      website
    }
    tags {
      name
    }
  }
}