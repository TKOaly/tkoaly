query GetJobs {
  jobs(orderBy: { startDate: desc}) {
    id
    title
    url
    content {
      document
    }
    startDate
    endDate
    company {
      id
      name
      website
    }
    tags {
      id
      name
    }
  }
}